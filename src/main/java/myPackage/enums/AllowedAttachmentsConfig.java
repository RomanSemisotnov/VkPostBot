package myPackage.enums;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.List;
import java.util.stream.Collectors;
import java.util.stream.Stream;

// Spring V
//@Configuration
public class AllowedAttachmentsConfig {
/*
    private enum ALLOWED_ATTACHMENTS {
        wall
    }

    @Bean
    public List<String> allowedAttachments() {
        return Stream.of(ALLOWED_ATTACHMENTS.values()).map(ALLOWED_ATTACHMENTS::toString)
                .collect(Collectors.toList());
    }
*/
}
